// to freeze the screen in the dom short cut key are (ctrl + shift + P)
// after that type command to freeze the screen type (emulate a focused page)
// then you can access the autosuggest elements and after locating the element
// stop freeze screen to type command (do not emulate a focused page)
// to make page as it is